import java.awt.Color;

public class Rules {
    Tile[][] board_with_tiles = Board.board_with_tiles;
    boolean valid = false;
    public boolean isMoveValid(Color now_playing, int x, int y){

        return valid;
    }

}
